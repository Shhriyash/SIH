import 'package:flutter/material.dart';

class MergedPinCodesPage extends StatelessWidget {
  const MergedPinCodesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}